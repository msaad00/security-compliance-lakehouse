import { Placeholder } from "@/components/Placeholder";

export default function ConnectorsPage() {
  return (
    <Placeholder
      title="Connectors"
      body="Read-only-first integrations into existing lakes, security tools, and identity systems with scoped credentials."
    />
  );
}
